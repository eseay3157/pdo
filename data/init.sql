CREATE DATABASE TEAM_VAM;

use TEAM_VAM;

CREATE TABLE users (
	id INT(11) UNSIGNED AUTO_INCREMENT PRIMARY KEY, 
	firstname VARCHAR(30) NOT NULL,
	lastname VARCHAR(30) NOT NULL,
	userid VARCHAR(50) NOT NULL,
	house VARCHAR(50),
	location VARCHAR(50),
	date TIMESTAMP
);